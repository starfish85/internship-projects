这份任务已经实现了一个只读采集脚本，入口对齐东京站点，能在可见浏览器里走到购票流程、在人工检验处等待，并能从已打开日历页或保存页面文本读取数据。
脚本可用 `--only-ticket Adult --target-month 2026-11` 限定票种和月份，本次 11 月 Adult 结果已输出到 `03_实习生提交/output_november_current_page/`，新的默认输出会写到 `03_实习生提交/generated/runs/`。
目前未实现自动通过人工检验，也没有强行推断官网未展示的库存数字；`currency` 已加入 CSV/JSON，日历页未直接显示时间段时，结果中 `time_slot` 保持为空。
